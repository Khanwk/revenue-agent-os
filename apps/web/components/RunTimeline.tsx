import type {RunEvent} from "@/types";
export function RunTimeline({events}:{events:RunEvent[]}){return <div className="trace"><div className="trace-head"><span>LIVE TRACE</span><small>{events.length?`${events.length} events`:"waiting"}</small></div><div className="trace-list">{events.length===0?<div className="trace-empty">Agent activity will appear here.</div>:events.slice(-6).map((event)=><div className={`trace-row trace-${event.status}`} key={event.id}><i/><div><strong>{event.label}</strong><p>{event.message}</p>{event.detail&&<small>{event.detail}</small>}</div></div>)}</div></div>}
